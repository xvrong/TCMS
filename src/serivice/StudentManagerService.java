package serivice;

public interface StudentManagerService {
    // void showStudentBasicInformation() {
    // // TODO: showBasicInformation of Student
    // }

    // void showCertainClassStudent() {
    // // select allStudent in a class
    // Get get = new Get();
    // Query qy = new Query();
    // Print pt = new Print();
    // Education edu = get.getInputEducation();
    // ArrayList<String> courseList = qy.queryCourse(edu);
    // String course = get.getInputCourse(courseList);
    // ArrayList<String> classList = qy.queryClass(edu, course);
    // String sclass = get.getInputClass(classList);
    // ArrayList<Pair<String, String>> studentList = qy.queryStudent(edu, course,
    // sclass);
    // pt.printStudent(studentList);
    // }

}
